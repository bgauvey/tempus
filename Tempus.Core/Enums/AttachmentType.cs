namespace Tempus.Core.Enums;

public enum AttachmentType
{
    File,    // Uploaded file (documents, PDFs, etc.)
    Link,    // External link (URLs to notes, documents, etc.)
    Image    // Uploaded image (for preview/display)
}
