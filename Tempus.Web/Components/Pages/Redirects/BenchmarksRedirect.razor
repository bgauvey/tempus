@page "/benchmarks"
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        // Redirect to unified Insights page
        NavigationManager.NavigateTo("/insights", replace: true);
    }
}
